/**
 * More features route ("/more"). Thin wrapper around the secondary tools screen.
 */
import React from 'react';

import { MoreFeaturesScreen } from '@/features/mobile/MoreFeaturesScreen';

export default function MoreRoute(): React.JSX.Element {
  return <MoreFeaturesScreen />;
}
