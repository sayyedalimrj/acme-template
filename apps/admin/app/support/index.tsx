/** Support Inbox route ("/support"). Internal-only, mock-only. */
import React from 'react';

import { SupportInboxScreen } from '@/features/SupportInboxScreen';

export default function SupportRoute(): React.JSX.Element {
  return <SupportInboxScreen />;
}
