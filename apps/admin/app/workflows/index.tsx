/** Workflow Operations board route ("/workflows"). Internal-only, mock-only. */
import React from 'react';

import { WorkflowBoardScreen } from '@/features/WorkflowBoardScreen';

export default function WorkflowsRoute(): React.JSX.Element {
  return <WorkflowBoardScreen />;
}
