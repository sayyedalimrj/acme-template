# SPA fallback for portal static hosts.
location = /config.json {
    add_header Cache-Control "no-store";
    try_files $uri =404;
}

location = /sw.js {
    add_header Cache-Control "no-cache";
    try_files $uri =404;
}

location / {
    try_files $uri $uri/ /index.html;
}
